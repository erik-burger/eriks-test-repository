name: Greetings

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  show:
    runs-on: ubuntu-latest
    steps:
      - name: Is variable exported?
        run: |
          cat << OBJECT
          ${{ toJson(github) }}
          OBJECT
