name: Manual workflow
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: deploy
      run: echo deploy
  
    
  deploy-teams:
    runs-on: ubuntu-latest
    needs:
      - deploy
    steps:
    - name: teams deploy message
      run: echo deploy är ok
  
  tests:
    runs-on: ubuntu-latest
    needs:
      - deploy
    steps:
    - name: Try to fail
      run: exit 1
      
  tests-teams:
    runs-on: ubuntu-latest
    needs:
      - tests
    steps:
    - name: teams deploy message
      run: echo deploy är ok
